@model AccountRecoveryMailTemplate
@{
    Layout = Model.GetLayoutFilePathForTextTemplate();
}
Hello @Model.User.DisplayName

We have received a request to reset the password for your member account on the @Model.ApplicationName sample site. You can reset your password by visiting @Model.RecoveryUrl and following the instructions.