import { Component, Input } from '@angular/core';
import { SELECTOR_PREFIX } from '../../constants/config.constants';

@Component({
	selector: SELECTOR_PREFIX + 'page-row',
	styleUrls: ['./page-row.component.scss'],
	templateUrl: 'page-row.component.html',
})

export default class PageRow {

}
