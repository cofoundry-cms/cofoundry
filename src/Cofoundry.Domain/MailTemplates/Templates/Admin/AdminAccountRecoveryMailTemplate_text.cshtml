@model AccountRecoveryMailTemplate
@{
    Layout = Model.GetLayoutFilePathForTextTemplate();
}
Hello,

We have received a request to reset the password for your account on the @Model.ApplicationName admin panel. Follow this link to to set a new password:

@Model.RecoveryUrl