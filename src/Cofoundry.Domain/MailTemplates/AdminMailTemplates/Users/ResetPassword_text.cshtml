@model ResetPasswordTemplate
@inject ICofoundryMailTemplateHelper Cofoundry
@inject Microsoft.Extensions.Configuration.IConfiguration configuration

@{
    Layout = TemplatePath.DefaultLayoutPath_Text;
    var applicationName = Cofoundry.Sanitizer.Sanitize(Model.ApplicationName);
}

Hi @Cofoundry.Sanitizer.Sanitize(Model.FirstName),

We have received a request to reset the password for your account on the @applicationName site. You can reset your password by visiting @Html.Raw(Cofoundry.Routing.ToAbsolute(UrlLibrary.PasswordReset(Model, configuration))) and following the instructions.

This message was automatically generated by the @applicationName site.