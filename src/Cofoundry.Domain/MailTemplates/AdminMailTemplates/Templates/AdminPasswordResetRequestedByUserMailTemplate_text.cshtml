@model AdminPasswordResetRequestedByUserMailTemplate
@inject ICofoundryMailTemplateHelper Cofoundry
@{
    Layout = Model.GetLayoutFilePathForTextTemplate();
}
Hello,

We have received a request to reset the password for your account on the @Model.ApplicationName admin panel.

You can reset your password by visiting @Cofoundry.Routing.ToAbsolute(Model.ResetUrl) and following the instructions.